[project]
name = "numpix"
version = "0.9.13"
description = "A simple, (almost) dependency free array visualiser for the terminal"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["numpy"]

[tool.setuptools.packages.find]
include = ["numpix*"]
